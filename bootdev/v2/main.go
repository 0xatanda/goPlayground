package main

import "fmt"

func main() {
	var smsSentLimit int
	var costPerSms float64
	var hasPermission bool
	var username string

	fmt.Printf("%v %f %v %q \n", smsSentLimit, costPerSms, hasPermission, username)
}
